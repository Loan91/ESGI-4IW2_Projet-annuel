{% extends 'base_front.html.twig' %}

{% block title %}Nouveau mot de passe{% endblock %}

{% block body %}
<section class="lg:w-1/3 mx-auto lg:pt-40 pt-10 px-4">
    <h1 class="font-bold text-2xl mb-4 text-center">Réinitialisation du mot de passe</h1>
    <p class="mb-4 text-md">Veuillez entrer votre nouveau mot de passe ci-dessous.</p>

    {% form_theme form 'themes/tailwindcss.html.twig' %}
    {{ form_start(form) }}
        {{ form_row(form.password) }}

        <input type="hidden" name="token" value="{{ token }}">
        <button type="submit" class="mt-6 w-full focus:outline-none text-white text-sm py-2.5 px-5 rounded-md bg-blue-500 hover:bg-blue-600 hover:shadow-lg mr-2 mb-4">
            Envoyer
        </button>
    {{ form_end(form) }}
</section>
    {# <form method="post">
        <label for="inputPassword">Password</label>
        <input type="password" name="password" id="inputPassword" placeholder="Mot de passe" required>
        
        <button type="submit" id="valider">
            Valider le mot de passe
        </button>
    </form> #}
{% endblock %}