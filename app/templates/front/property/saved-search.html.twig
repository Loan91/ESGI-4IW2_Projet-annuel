{% extends 'base_front.html.twig' %}

{% block body %}
    <main class="mx-4 mb-4 container mx-auto">
        <header class="mt-4 mb-6">
            <h1 class="text-5xl font-bold text-center">Recherches sauvegardées</h1>
        </header>

        <div>
            {% for saved_search in saved_searches %}
                {{ saved_search }}
            {% endfor %}
        </div>
    </main>
{% endblock %}